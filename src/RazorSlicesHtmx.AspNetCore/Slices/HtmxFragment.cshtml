@inherits RazorSlice<HtmxFragmentModel>

@(await RenderPartialAsync(Model.Primary))

@foreach (var part in Model.OobParts)
{
    @(await RenderPartialAsync(part))
}

@if (Model.RawOobParts is { Count: > 0 })
{
    @foreach (var raw in Model.RawOobParts)
    {
        @raw
    }
}