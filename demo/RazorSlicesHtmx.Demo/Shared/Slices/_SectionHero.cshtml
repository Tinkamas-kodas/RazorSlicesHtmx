@inherits RazorSlice<SectionHeroModel>

<div class="section-hero">
    <div>
        <p class="section-eyebrow">@Model.Eyebrow</p>
        <h2 class="section-title">@Model.Headline</h2>
    </div>
    <span class="section-route">@Model.Route</span>
</div>